## LET'S LEARN ABOUT PROPS

* Props is like whenever we make any components; that components have access of props already....

* We cannot directly pass **array or object** to the props.

* We have to declare a **variable**.

* And then we have to pass that variable into **{}**.
